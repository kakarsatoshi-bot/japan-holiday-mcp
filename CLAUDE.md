# japan-holiday-mcp

日本の祝日情報を返すMCPサーバー（内閣府公式CSVデータ準拠）。Cloudflare Workers + TypeScriptの最小構成。Smithery/Gumroad/Zennで展開する「Japan Dataシリーズ」MCPの1つ（1 MCP = 1リポジトリ）。

## セッション終わりの記録ルール（2026-07-29策定）

Claude Codeの使用量節約のため、セッションを長く続けず、区切りごとに記録を残して次のセッションへ引き継ぐ。

- **作業が未完了のまま終える場合**：セッションの終わりに「どこまで進んだか・次に何をやるか」を短く書き残す（会話ログを掘り起こさなくても次のセッションが続きから動けるように）。
- **ログへの追記**：本リポジトリには現状changelog／引き継ぎ書の類はない。作る場合も、仕組み（デプロイ設定・スキーマ等）に変更を加えた時だけ追記する運用とし、日常的な変更は対象外とする。
